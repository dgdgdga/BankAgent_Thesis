\documentclass[12pt,a4paper]{article}

% === Minimal packages ===
\usepackage{graphicx}
\usepackage{float}
\usepackage{geometry}
\usepackage{booktabs}
\usepackage{array}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{needspace}
\usepackage[hyphens]{url}
\usepackage{caption}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{ragged2e}

\captionsetup[figure]{
  font=bf,
  justification=centering,
  singlelinecheck=false
}

\captionsetup[table]{
  font={bf,small},
  justification=centering,
  singlelinecheck=false,
  skip=6pt,
  position=above
}

\urlstyle{same}
\setlength{\emergencystretch}{2em}
\tolerance=1000

\lstset{
  basicstyle=\small\ttfamily,
  breaklines=true,
  frame=tb,
  tabsize=2,
  numbers=left,
  numberstyle=\tiny\color{gray},
  aboveskip=6pt,
  belowskip=6pt,
  xleftmargin=12pt,
  framexleftmargin=8pt,
}
\geometry{left=30mm,right=30mm,top=25mm,bottom=25mm}
\usepackage{setspace}
\setstretch{1.5}
\usepackage{fontspec}
\setmainfont{Times New Roman}
\setlength{\parindent}{0pt}
\setlength{\parskip}{1ex plus 0.5ex minus 0.2ex}

% Allow line breaks in long identifiers and code blocks
\usepackage{hyphenat}

% Make \_ a legal line-break point
\let\oldus\_
\def\_{\oldus\penalty0\hskip 0pt plus 2pt}

% Table style: full-width, smaller font, compact
\usepackage{etoolbox}
\BeforeBeginEnvironment{table}{\small\singlespacing}
\AtBeginEnvironment{tabularx}{\small\singlespacing}
\setlength{\tabcolsep}{6pt}

% Sub-heading: uniform spacing above and below
\newcommand{\subhead}[1]{%
  \vspace{10pt}%
  {\fontsize{14}{17}\selectfont\textbf{#1}}%
  \vspace{6pt}\par%
}

% Sub-sub-heading
\newcommand{\subsubhead}[1]{%
  \vspace{8pt}%
  {\fontsize{13}{16}\selectfont\textbf{#1}}%
  \vspace{4pt}\par%
}

\begin{document}
