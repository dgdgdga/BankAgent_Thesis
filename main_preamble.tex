\documentclass[12pt,a4paper]{article}

% === Minimal packages ===
\usepackage{graphicx}
\usepackage{float}
\usepackage{geometry}
\usepackage{booktabs}
\usepackage{array}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{needspace}
\usepackage[hyphens]{url}
\usepackage[colorlinks=true,linkcolor=black,citecolor=black,urlcolor=black]{hyperref}
\usepackage{caption}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{ragged2e}
\usepackage{titlesec}
\usepackage{tocloft}
\renewcommand{\cfttoctitlefont}{\hfill\fontsize{16}{19}\selectfont\bfseries}
\renewcommand{\cftaftertoctitle}{\hfill}

% Section numbering depth
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

% Rename and style the table of contents heading
\renewcommand{\contentsname}{Content}

% Section: 18pt bold, same as original chapter headings
\titleformat{\section}
  {\fontsize{18}{22}\selectfont\bfseries}
  {\thesection\ }
  {0pt}
  {}
\titlespacing{\section}{0pt}{0pt}{3mm}

% Subsection: 14pt bold, same as original \subhead
\titleformat{\subsection}
  {\fontsize{14}{17}\selectfont\bfseries}
  {\thesubsection\ }
  {0pt}
  {}
\titlespacing{\subsection}{0pt}{10pt}{6pt}

% Subsubsection: 13pt bold, same as original \subsubhead
\titleformat{\subsubsection}
  {\fontsize{13}{16}\selectfont\bfseries}
  {\thesubsubsection\ }
  {0pt}
  {}
\titlespacing{\subsubsection}{0pt}{8pt}{4pt}

% Page style: footer with page number
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

\captionsetup[figure]{
  font=small,
  labelfont=normalfont,
  textfont=normalfont,
  justification=centering,
  singlelinecheck=false
}

\captionsetup[table]{
  font=small,
  labelfont=normalfont,
  textfont=normalfont,
  justification=centering,
  singlelinecheck=false,
  skip=6pt,
  position=above
}

\urlstyle{same}
\setlength{\emergencystretch}{2em}
\tolerance=1000

\lstset{
  basicstyle=\scriptsize\ttfamily,
  breaklines=true,
  frame=tb,
  tabsize=2,
  numbers=left,
  numberstyle=\tiny\color{gray},
  aboveskip=6pt,
  belowskip=6pt,
  xleftmargin=12pt,
  framexleftmargin=8pt,
}
\geometry{left=30mm,right=30mm,top=25mm,bottom=25mm}
\usepackage{setspace}
\setstretch{1.5}
\usepackage{fontspec}
\setmainfont{Times New Roman}
\setlength{\parindent}{0pt}
\setlength{\parskip}{1ex plus 0.5ex minus 0.2ex}

% Allow line breaks in long identifiers and code blocks
\usepackage{hyphenat}

% Make \_ a legal line-break point
\let\oldus\_
\def\_{\oldus\penalty0\hskip 0pt plus 2pt}

% Table style: full-width, smaller font, compact
\usepackage{etoolbox}
\setlength{\tabcolsep}{6pt}

% Sub-heading: uniform spacing above and below
\newcommand{\subhead}[1]{%
  \vspace{10pt}%
  {\fontsize{14}{17}\selectfont\textbf{#1}}%
  \vspace{6pt}\par%
}

% Sub-sub-heading
\newcommand{\subsubhead}[1]{%
  \vspace{8pt}%
  {\fontsize{13}{16}\selectfont\textbf{#1}}%
  \vspace{4pt}\par%
}

\begin{document}
